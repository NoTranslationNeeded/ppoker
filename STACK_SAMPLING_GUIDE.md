# 스택 샘플링 전략

## 🎯 핵심 원칙

### 매 핸드 리셋 + 스마트 샘플링

**문제: 토너먼트 방식**
```
Start: 500BB
Hand 1-400: 딥스택 → 미들스택
Hand 401-500: 숏스택만!

결과:
- AI가 숏스택만 학습
- 딥스택 경험 부족
- 시간 낭비
```

**해결: 매 핸드 리셋**
```
Hand 1: 100BB
Hand 2: 35BB
Hand 3: 180BB
Hand 4: 12BB
...

결과:
- 모든 스택 깊이 균등 학습
- 효율적
```

---

## 📊 스택 분포

### 중요도 기반 샘플링

```python
def _sample_stack_depth(self) -> float:
    """스택 깊이 중요도 기반 샘플링"""
    categories = {
        'standard': (80, 120, 0.40),   # 100BB ±20
        'middle': (20, 50, 0.30),      # 20-50BB
        'short': (5, 20, 0.20),        # 5-20BB
        'deep': (150, 250, 0.10)       # 150-250BB
    }
    
    # 40% 확률로 standard
    # 30% 확률로 middle
    # 20% 확률로 short
    # 10% 확률로 deep
    
    category = np.random.choice(
        ['standard', 'middle', 'short', 'deep'],
        p=[0.40, 0.30, 0.20, 0.10]
    )
    
    min_bb, max_bb, _ = categories[category]
    stack_bb = np.random.uniform(min_bb, max_bb)
    
    return stack_bb * self.big_blind
```

---

## 🎲 확률 분포

| 카테고리 | BB 범위 | 확률 | 이유 |
|---------|---------|------|------|
| **Standard** | 80-120 | 40% | 표준 캐시게임, 토너먼트 초반 |
| **Middle** | 20-50 | 30% | 토너먼트 중반 |
| **Short** | 5-20 | 20% | Push/Fold, 토너먼트 후반 |
| **Deep** | 150-250 | 10% | 딥스택 전략 |

---

## 📈 10,000 핸드 예상 분포

```
Standard (100BB): 4,000 핸드
- 85BB: 800핸드
- 95BB: 800핸드
- 100BB: 900핸드
- 105BB: 800핸드
- 115BB: 700핸드

Middle (20-50BB): 3,000 핸드
- 25BB: 600핸드
- 35BB: 800핸드
- 45BB: 600핸드
- ...

Short (5-20BB): 2,000 핸드
- 10BB: 600핸드
- 15BB: 700핸드
- ...

Deep (150-250BB): 1,000 핸드
- 180BB: 400핸드
- 200BB: 400핸드
- ...
```

---

## 🔧 BB 고정

### 왜 BB=100?

```
BB=1, Stack=500 = 500BB
BB=100, Stack=50000 = 500BB
BB=10000, Stack=5000000 = 500BB

→ 수학적으로 완전히 동일!
→ BB 절대값은 의미 없음
→ BB 단위만 중요
```

**고정값**:
- SB = 50
- BB = 100

**스택 예시**:
- 100BB = 10,000 칩
- 50BB = 5,000 칩
- 200BB = 20,000 칩

---

## ⚠️ 균등 분포의 문제

```python
# ❌ 균등 분포 (문제!)
stack = np.random.uniform(5, 250)
# 결과: 137BB, 213BB, 73BB, ...
# 문제: 실제로 잘 안 나오는 상황

# ✅ 중요도 기반 (좋음!)
# 100BB: 40%
# 35BB: 자주
# 189BB: 가끔
```

---

## 💡 장점

1. **효율성**: 1핸드 = 1에피소드 (빠름)
2. **균형**: 모든 스택 깊이 학습
3. **현실성**: 실제 중요한 상황에 집중
4. **정규화**: BB=100 고정으로 간단

---

## 🎯 구현

```python
def reset(self):
    # 스택 샘플링
    stack_0 = self._sample_stack_depth()
    stack_1 = self._sample_stack_depth()
    
    self.chips = [stack_0, stack_1]
    
    # 핸드 시작
    self._start_new_hand()
    
    return obs, info

def step(self, action_dict):
    # 핸드 처리
    ...
    
    # 핸드 종료 시
    if self.game.is_hand_over:
        done = True  # 즉시 종료
        # 다음 핸드는 reset()에서!
    
    return obs, reward, done, info
```
